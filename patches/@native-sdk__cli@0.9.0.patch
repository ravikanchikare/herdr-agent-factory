diff --git a/src/agent_factory_terminal_tests_root.zig b/src/agent_factory_terminal_tests_root.zig
new file mode 100644
index 0000000000000000000000000000000000000000..379b9404c56830d68dc830b40294ff71d571f085
--- /dev/null
+++ b/src/agent_factory_terminal_tests_root.zig
@@ -0,0 +1,8 @@
+//! Consumer-side terminal input regressions. These need an application build
+//! that wires libghostty-vt, so Agent Factory roots them beside its own native
+//! tests instead of relying on the SDK's emulator-free test graph.
+
+test {
+    _ = @import("runtime/terminal_session_tests.zig");
+    _ = @import("runtime/canvas_widget_context_menu_tests.zig");
+}
diff --git a/src/primitives/canvas/terminal_grid.zig b/src/primitives/canvas/terminal_grid.zig
index f36fa7d8cbaed57ec7e67ecd75cb19bc1a33d38c..04d91d6242df7489aeddfb7550cf9201dbd4a587 100644
--- a/src/primitives/canvas/terminal_grid.zig
+++ b/src/primitives/canvas/terminal_grid.zig
@@ -133,6 +133,11 @@ pub const TerminalGrid = struct {
     /// The session is live: the cursor paints filled; an ended session
     /// paints it hollow-dim, the honest at-rest state.
     running: bool = true,
+    /// The child enabled a terminal mouse-reporting mode. This remains
+    /// framework-owned: pointer routing uses it to let the terminal
+    /// application receive secondary-button gestures instead of opening the
+    /// host's fallback Copy/Paste menu.
+    mouse_reporting: bool = false,
     /// Keyboard-selection caret: while selection mode is armed the head
     /// cell paints a focus outline (the selection wash itself rides the
     /// rows' `selection` ranges).
diff --git a/src/runtime/canvas_widget_context_menu.zig b/src/runtime/canvas_widget_context_menu.zig
index d69aafa2e6ab46b233694a2ec671553a2b239023..b3cabda001e34d0ade22529e1da43463cee53a46 100644
--- a/src/runtime/canvas_widget_context_menu.zig
+++ b/src/runtime/canvas_widget_context_menu.zig
@@ -93,12 +93,32 @@ pub fn RuntimeCanvasWidgetContextMenu(comptime Runtime: type) type {
         /// must be consumed by the context-menu path instead of the
         /// primary pointer pipeline (a right-click must never act as a
         /// press).
-        pub fn canvasWidgetContextPointerInput(input_event: platform.GpuSurfaceInputEvent) bool {
+        pub fn canvasWidgetContextPointerInput(self: *Runtime, input_event: platform.GpuSurfaceInputEvent) bool {
             if (input_event.button != 1) return false;
-            return switch (input_event.kind) {
+            const secondary = switch (input_event.kind) {
                 .pointer_down, .pointer_up, .pointer_drag, .pointer_move, .pointer_cancel => true,
                 else => false,
             };
+            if (!secondary or input_event.modifiers.shift) return secondary;
+
+            // A terminal application that enabled mouse reporting owns the
+            // secondary-button stream. The grid snapshot is framework state,
+            // so this routing decision does not leak emulator modes into the
+            // app-visible TerminalState. Shift deliberately keeps the native
+            // Copy/Paste menu as the local override.
+            const routed = CanvasWidgetEventMethods().routeCanvasWidgetPointerInput(
+                self,
+                input_event,
+                &self.widget_event_route_entries,
+            ) catch return true;
+            const pointer_event = routed orelse return true;
+            const target = pointer_event.target orelse return true;
+            const index = runtimeFindViewIndex(self, input_event.window_id, input_event.label) orelse return true;
+            const node_index = self.views[index].canvasWidgetNodeIndexById(target.id) orelse return true;
+            const widget = self.views[index].widget_layout_nodes[node_index].widget;
+            if (widget.kind != .terminal) return true;
+            const grid = widget.terminal.grid orelse return true;
+            return !grid.mouse_reporting;
         }
 
         /// Present the context menu for a secondary-button press: hit-test
diff --git a/src/runtime/canvas_widget_context_menu_tests.zig b/src/runtime/canvas_widget_context_menu_tests.zig
index a5a4fa112a8c7cdca050ff86ad2e6c22b518205e..88c25c5384189bc5b74cf54e8c24d73fa87f0f85 100644
--- a/src/runtime/canvas_widget_context_menu_tests.zig
+++ b/src/runtime/canvas_widget_context_menu_tests.zig
@@ -545,6 +545,40 @@ test "right click on a terminal presents Copy and Paste wired to selection and c
     try std.testing.expectEqualStrings(shortcut_paste, app_state.last_edit_insert[0..app_state.last_edit_insert_len]);
 }
 
+test "right click bypasses the native menu while a terminal captures mouse input" {
+    var app_state: MenuTestApp = .{};
+    const app = app_state.app();
+    const harness = try createMenuHarness(app);
+    defer harness.destroy(std.testing.allocator);
+
+    var grid = canvas.TerminalGrid{
+        .background = canvas.Color.rgba(0, 0, 0, 1),
+        .foreground = canvas.Color.rgba(1, 1, 1, 1),
+        .cursor_color = canvas.Color.rgba(1, 1, 1, 1),
+        .selection_color = canvas.Color.rgba(0, 0.5, 1, 1),
+        .mouse_reporting = true,
+    };
+    const terminal = canvas.Widget{
+        .id = 2,
+        .kind = .terminal,
+        .frame = geometry.RectF.init(12, 16, 280, 120),
+        .terminal = .{ .pty = 7, .grid = &grid },
+    };
+    var nodes: [2]canvas.WidgetLayoutNode = undefined;
+    const layout = try canvas.layoutWidgetTree(.{ .kind = .stack, .children = &.{terminal} }, geometry.RectF.init(0, 0, 320, 200), &nodes);
+    _ = try harness.runtime.setCanvasWidgetLayout(1, "canvas", layout);
+
+    try harness.runtime.dispatchPlatformEvent(app, rightClick(100, 40));
+    try std.testing.expectEqual(@as(usize, 0), harness.null_platform.context_menu_request_count);
+    try std.testing.expectEqual(@as(u32, 1), app_state.pointer_count);
+
+    // Shift-right-click is the explicit host-menu override.
+    var shifted = rightClick(100, 40);
+    shifted.gpu_surface_input.modifiers.shift = true;
+    try harness.runtime.dispatchPlatformEvent(app, shifted);
+    try std.testing.expectEqual(@as(usize, 1), harness.null_platform.context_menu_request_count);
+}
+
 test "terminal Paste disables after exit and a pending live menu revalidates before dispatch" {
     var app_state: MenuTestApp = .{};
     const app = app_state.app();
diff --git a/src/runtime/gpu_surface_events.zig b/src/runtime/gpu_surface_events.zig
index 1d869f4faa2451be3a7a32680393818a54d639d9..884d8017b7100ae5f37a46b537a15e7895f029ff 100644
--- a/src/runtime/gpu_surface_events.zig
+++ b/src/runtime/gpu_surface_events.zig
@@ -210,7 +210,7 @@ pub fn RuntimeGpuSurfaceEvents(comptime Runtime: type) type {
             // is the context-menu gesture: the press presents the
             // native menu and the whole button-1 stream is consumed so a
             // right-click never acts as a primary press.
-            if (ContextMenuMethods().canvasWidgetContextPointerInput(input_event)) {
+            if (ContextMenuMethods().canvasWidgetContextPointerInput(self, input_event)) {
                 if (runtimeFindViewIndex(self, input_event.window_id, input_event.label)) |index| {
                     self.views[index].recordGpuSurfaceInputTimestamp(input_event.timestamp_ns);
                     // A consumed cancel is still the pointer leaving the
diff --git a/src/runtime/terminal_session.zig b/src/runtime/terminal_session.zig
index 808265e428108119c6b214152d659b454e64e340..e57fd93066de5b9acbe237dc4507abfcbe1a4497 100644
--- a/src/runtime/terminal_session.zig
+++ b/src/runtime/terminal_session.zig
@@ -54,22 +54,34 @@ pub const PtyGateway = struct {
     resize: *const fn (context: *anyopaque, key: u64, cols: u16, rows: u16) void,
 };
 
-/// One pointer event already translated into the terminal's padded
-/// content box. Coordinates remain unclamped so a captured drag beyond
-/// an edge extends to that edge; the session maps them onto its live
-/// emulator grid using the same cell metrics the snapshot painter uses.
+/// One pointer event already translated into the terminal's padded content
+/// box. The session either encodes it for a child that enabled terminal mouse
+/// reporting or applies it to the emulator's local selection model.
 pub const PointerSelectionEvent = struct {
     phase: canvas.WidgetPointerPhase,
     x: f32,
     y: f32,
     width: f32,
     height: f32,
+    button: i32 = 0,
+    modifiers: canvas.WidgetKeyboardModifiers = .{},
     click_count: u8 = 1,
 };
 
 pub const PointerSelectionResult = struct {
     changed: bool = false,
     selection_active: bool = false,
+    mouse_reported: bool = false,
+};
+
+pub const PointerWheelEvent = struct {
+    x: f32,
+    y: f32,
+    width: f32,
+    height: f32,
+    delta_x: f32 = 0,
+    delta_y: f32 = 0,
+    modifiers: canvas.WidgetKeyboardModifiers = .{},
 };
 
 pub const TerminalSessions = if (enabled) EnabledStore else DisabledStore;
@@ -130,6 +142,12 @@ const DisabledStore = struct {
         _ = delta_y;
         return false;
     }
+    pub fn mouseWheel(self: *DisabledStore, pty: u64, event: PointerWheelEvent) bool {
+        _ = self;
+        _ = pty;
+        _ = event;
+        return false;
+    }
     pub fn pointerSelection(self: *DisabledStore, pty: u64, event: PointerSelectionEvent) PointerSelectionResult {
         _ = self;
         _ = pty;
@@ -279,6 +297,7 @@ const EnabledStore = struct {
             .exit => {
                 session.ended = true;
                 session.grid.running = false;
+                session.grid.mouse_reporting = false;
                 // The child is gone: queued bytes can never land — drop
                 // them counted, and clear retained emulator replies (the
                 // same outbound loss), or the frame flush would retry
@@ -385,12 +404,21 @@ const EnabledStore = struct {
         return true;
     }
 
-    /// Primary-pointer terminal selection. The emulator owns the
-    /// tracked pins and selection serialization; the canvas runtime
-    /// supplies only the gesture and the content-box geometry.
+    /// A wheel over a child that enabled mouse reporting belongs to that
+    /// child. Small trackpad deltas accumulate into cell-sized protocol
+    /// ticks; returning true means the gesture was captured even when this
+    /// individual delta did not yet complete a tick.
+    pub fn mouseWheel(self: *EnabledStore, pty: u64, event: PointerWheelEvent) bool {
+        const session = self.find(pty) orelse return false;
+        return session.mouseWheel(self.gateway, pty, event);
+    }
+
+    /// Terminal pointer input. A child mouse mode owns the gesture unless
+    /// Shift requests the host selection override; otherwise the emulator owns
+    /// primary-button selection and its serialized clipboard text.
     pub fn pointerSelection(self: *EnabledStore, pty: u64, event: PointerSelectionEvent) PointerSelectionResult {
         const session = self.find(pty) orelse return .{};
-        const result = session.pointerSelection(event);
+        const result = session.pointerInput(self.gateway, pty, event);
         if (result.changed) session.snapshot_dirty = true;
         return result;
     }
@@ -543,6 +571,10 @@ const Session = if (enabled) struct {
     last_bound_scrollback: u32 = 0,
     last_reported: ?canvas.TerminalState = null,
     wheel_accum: f32 = 0,
+    mouse_wheel_accum_x: f32 = 0,
+    mouse_wheel_accum_y: f32 = 0,
+    mouse_pressed_buttons: u16 = 0,
+    mouse_last_cell: ?vt.Coordinate = null,
     /// Cell metrics at the last snapshot's tokens, shared by wheel
     /// delta-to-rows and pointer-to-cell selection.
     cell_width: f32 = 8,
@@ -654,6 +686,10 @@ const Session = if (enabled) struct {
         session.outbound_len = 0;
         session.outbound_dropped = 0;
         session.wheel_accum = 0;
+        session.mouse_wheel_accum_x = 0;
+        session.mouse_wheel_accum_y = 0;
+        session.mouse_pressed_buttons = 0;
+        session.mouse_last_cell = null;
         session.macos_natural_keys_held = 0;
         session.ended = false;
         session.grid.running = true;
@@ -850,7 +886,7 @@ const Session = if (enabled) struct {
         return session.term.screens.active.pages.scrollbar();
     }
 
-    // ------------------------------------------------ pointer selection
+    // --------------------------------------------------------- pointer input
 
     /// Default terminal word boundaries, matching Ghostty's standard
     /// selection gesture. Kept local because the pinned vt module
@@ -863,6 +899,42 @@ const Session = if (enabled) struct {
         '}', '<', '>',  '$',
     };
 
+    fn pointerInput(session: *Session, gateway: ?PtyGateway, pty_key: u64, event: PointerSelectionEvent) PointerSelectionResult {
+        if (session.mouseReporting(event.modifiers)) {
+            session.clearSelectionForInput();
+            const button = mouseButton(event.button);
+            switch (event.phase) {
+                .down => {
+                    session.setMouseButton(event.button, true);
+                    session.reportMouse(gateway, pty_key, event, .press, button);
+                },
+                .up => {
+                    session.setMouseButton(event.button, false);
+                    session.reportMouse(gateway, pty_key, event, .release, button);
+                },
+                .move, .hover => session.reportMouse(
+                    gateway,
+                    pty_key,
+                    event,
+                    .motion,
+                    session.firstPressedMouseButton(),
+                ),
+                .cancel => {
+                    session.setMouseButton(event.button, false);
+                    session.reportMouse(gateway, pty_key, event, .release, button);
+                    session.mouse_pressed_buttons = 0;
+                    session.mouse_last_cell = null;
+                },
+                .wheel => {},
+            }
+            return .{ .mouse_reported = true };
+        }
+        // Only the primary button selects locally. Shift+right-click remains
+        // available to the native Copy/Paste context menu.
+        if (event.button != 0) return .{};
+        return session.pointerSelection(event);
+    }
+
     fn pointerSelection(session: *Session, event: PointerSelectionEvent) PointerSelectionResult {
         if (!std.math.isFinite(event.x) or !std.math.isFinite(event.y) or
             !std.math.isFinite(event.width) or !std.math.isFinite(event.height) or
@@ -937,6 +1009,111 @@ const Session = if (enabled) struct {
         }
     }
 
+    fn mouseReporting(session: *const Session, modifiers: canvas.WidgetKeyboardModifiers) bool {
+        return session.acceptsInput() and
+            session.term.flags.mouse_event != .none and
+            !modifiers.shift;
+    }
+
+    fn mouseWheel(session: *Session, gateway: ?PtyGateway, pty_key: u64, event: PointerWheelEvent) bool {
+        if (!session.mouseReporting(event.modifiers)) return false;
+        if (!std.math.isFinite(event.delta_x) or !std.math.isFinite(event.delta_y)) return true;
+        session.clearSelectionForInput();
+        session.mouse_wheel_accum_x += event.delta_x;
+        session.mouse_wheel_accum_y += event.delta_y;
+        const cell_w = @max(1, session.cell_width);
+        const cell_h = @max(1, session.cell_height);
+        const x_ticks: isize = @intFromFloat(@trunc(session.mouse_wheel_accum_x / cell_w));
+        const y_ticks: isize = @intFromFloat(@trunc(session.mouse_wheel_accum_y / cell_h));
+        session.mouse_wheel_accum_x -= @as(f32, @floatFromInt(x_ticks)) * cell_w;
+        session.mouse_wheel_accum_y -= @as(f32, @floatFromInt(y_ticks)) * cell_h;
+        const pointer = PointerSelectionEvent{
+            .phase = .wheel,
+            .x = event.x,
+            .y = event.y,
+            .width = event.width,
+            .height = event.height,
+            .modifiers = event.modifiers,
+        };
+        for (0..@min(@as(usize, @intCast(@abs(y_ticks))), 32)) |_| {
+            session.reportMouse(gateway, pty_key, pointer, .press, if (y_ticks > 0) .four else .five);
+        }
+        for (0..@min(@as(usize, @intCast(@abs(x_ticks))), 32)) |_| {
+            session.reportMouse(gateway, pty_key, pointer, .press, if (x_ticks > 0) .six else .seven);
+        }
+        return true;
+    }
+
+    fn reportMouse(
+        session: *Session,
+        gateway: ?PtyGateway,
+        pty_key: u64,
+        event: PointerSelectionEvent,
+        action: vt.input.MouseAction,
+        button: ?vt.input.MouseButton,
+    ) void {
+        const screen_width: u32 = @intFromFloat(@max(1, @round(event.width)));
+        const screen_height: u32 = @intFromFloat(@max(1, @round(event.height)));
+        var options: vt.input.MouseEncodeOptions = .fromTerminal(&session.term, .{
+            .screen = .{ .width = screen_width, .height = screen_height },
+            .cell = .{
+                .width = @intFromFloat(@max(1, @round(session.cell_width))),
+                .height = @intFromFloat(@max(1, @round(session.cell_height))),
+            },
+            .padding = .{},
+        });
+        options.any_button_pressed = session.mouse_pressed_buttons != 0;
+        options.last_cell = &session.mouse_last_cell;
+        var buffer: [128]u8 = undefined;
+        var writer: std.Io.Writer = .fixed(&buffer);
+        vt.input.encodeMouse(&writer, .{
+            .action = action,
+            .button = button,
+            .mods = .{
+                .shift = event.modifiers.shift,
+                .ctrl = event.modifiers.control,
+                .alt = event.modifiers.alt,
+                .super = event.modifiers.super,
+            },
+            .pos = .{ .x = event.x, .y = event.y },
+        }, options) catch return;
+        if (writer.end > 0) session.enqueueTransient(gateway, pty_key, buffer[0..writer.end]);
+    }
+
+    fn setMouseButton(session: *Session, button: i32, pressed: bool) void {
+        if (button < 0 or button >= 16) return;
+        const bit = @as(u16, 1) << @intCast(button);
+        if (pressed) {
+            session.mouse_pressed_buttons |= bit;
+        } else {
+            session.mouse_pressed_buttons &= ~bit;
+        }
+    }
+
+    fn firstPressedMouseButton(session: *const Session) ?vt.input.MouseButton {
+        for (0..16) |button| {
+            if ((session.mouse_pressed_buttons & (@as(u16, 1) << @intCast(button))) != 0) {
+                return mouseButton(@intCast(button));
+            }
+        }
+        return null;
+    }
+
+    fn mouseButton(button: i32) ?vt.input.MouseButton {
+        return switch (button) {
+            0 => .left,
+            1 => .right,
+            2 => .middle,
+            3 => .four,
+            4 => .five,
+            5 => .six,
+            6 => .seven,
+            7 => .eight,
+            8 => .nine,
+            else => null,
+        };
+    }
+
     fn applyPointerDrag(session: *Session, event: PointerSelectionEvent) bool {
         const pin = session.pointerPin(event) orelse return false;
         const selection = session.selection_gesture.drag(&session.term, .{
@@ -1391,6 +1568,7 @@ const Session = if (enabled) struct {
                 };
             } else null,
             .running = !session.ended,
+            .mouse_reporting = session.term.flags.mouse_event != .none,
             .select_head = null,
             .scrollbar = .{
                 .offset = @intCast(bar.offset),
diff --git a/src/runtime/terminal_session_tests.zig b/src/runtime/terminal_session_tests.zig
index 951dbaaf4658259666c019ce5eacbf1bce8c7d5a..5600dd3b15b3e480061888c0e0c319c791860690 100644
--- a/src/runtime/terminal_session_tests.zig
+++ b/src/runtime/terminal_session_tests.zig
@@ -169,6 +169,94 @@ test "pointer gestures select terminal cells words and lines for clipboard copy"
     try testing.expectEqualStrings("", grid.selection_text);
 }
 
+test "terminal mouse modes receive clicks right clicks motion and wheel reports" {
+    if (comptime !terminal_session.enabled) return error.SkipZigTest;
+    var store = TerminalSessions.init(testing.allocator);
+    defer store.deinit();
+    var gw = TestGateway{ .gpa = testing.allocator };
+    defer gw.deinit();
+    store.setGateway(gw.gateway());
+    store.beginBuild(.{});
+    _ = store.reconcile(18, 0, 20, 4) orelse return error.TestExpectedState;
+
+    // Herdr-style TUIs negotiate button-motion tracking and the SGR format.
+    feedOutput(&store, 18, "\x1b[?1002h\x1b[?1006h");
+    const grid = resolveGrid(&store, 18) orelse return error.TestExpectedGrid;
+    try testing.expect(grid.mouse_reporting);
+
+    var result = store.pointerSelection(18, .{
+        .phase = .down,
+        .x = 2,
+        .y = 5,
+        .width = 160,
+        .height = 72,
+    });
+    try testing.expect(result.mouse_reported);
+    result = store.pointerSelection(18, .{
+        .phase = .move,
+        .x = 10,
+        .y = 5,
+        .width = 160,
+        .height = 72,
+    });
+    try testing.expect(result.mouse_reported);
+    result = store.pointerSelection(18, .{
+        .phase = .up,
+        .x = 10,
+        .y = 5,
+        .width = 160,
+        .height = 72,
+    });
+    try testing.expect(result.mouse_reported);
+    try testing.expectEqualStrings(
+        "\x1b[<0;1;1M\x1b[<32;2;1M\x1b[<0;2;1m",
+        gw.written.items,
+    );
+
+    gw.written.clearRetainingCapacity();
+    _ = store.pointerSelection(18, .{
+        .phase = .down,
+        .x = 18,
+        .y = 23,
+        .width = 160,
+        .height = 72,
+        .button = 1,
+    });
+    _ = store.pointerSelection(18, .{
+        .phase = .up,
+        .x = 18,
+        .y = 23,
+        .width = 160,
+        .height = 72,
+        .button = 1,
+    });
+    try testing.expectEqualStrings("\x1b[<2;3;2M\x1b[<2;3;2m", gw.written.items);
+
+    gw.written.clearRetainingCapacity();
+    try testing.expect(store.mouseWheel(18, .{
+        .x = 2,
+        .y = 5,
+        .width = 160,
+        .height = 72,
+        .delta_y = 18,
+    }));
+    try testing.expectEqualStrings("\x1b[<64;1;1M", gw.written.items);
+
+    // Shift is the local override: no bytes reach the child and ordinary
+    // emulator selection remains available.
+    gw.written.clearRetainingCapacity();
+    result = store.pointerSelection(18, .{
+        .phase = .down,
+        .x = 2,
+        .y = 5,
+        .width = 160,
+        .height = 72,
+        .modifiers = .{ .shift = true },
+    });
+    try testing.expect(!result.mouse_reported);
+    try testing.expectEqualStrings("", gw.written.items);
+}
+
 test "committed text and encoded keys reach the pty through the gateway in stdin order" {
     if (comptime !terminal_session.enabled) return error.SkipZigTest;
     var store = TerminalSessions.init(testing.allocator);
diff --git a/src/runtime/ui_app.zig b/src/runtime/ui_app.zig
index ee3f245b66174afc6b7fc8fd1e3f01dd8c9d717e..e77e14a9e792b1b949de0b654bfa4dc94490fbf6 100644
--- a/src/runtime/ui_app.zig
+++ b/src/runtime/ui_app.zig
@@ -4646,20 +4646,29 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
             return map(chrome);
         }
 
-        /// Route primary pointer selection into a bound terminal before
-        /// ordinary press dispatch. The layout query supplies the
-        /// terminal's resolved frame; coordinates are translated into
-        /// the same padded content box the painter and size reconcile
-        /// use, then Ghostty owns cell snapping and tracked selection
-        /// pins. Returns whether an active selection should suppress a
-        /// release-time `on_press`.
+        fn terminalContentRect(node: canvas.WidgetLayoutNode) geometry.RectF {
+            const frame = node.frame.normalized();
+            const padding = node.widget.layout.padding;
+            const declared = padding.left + padding.top + padding.right + padding.bottom > 0;
+            const inset: geometry.InsetsF = if (declared) padding else geometry.InsetsF.all(8);
+            return geometry.RectF.init(
+                frame.x + inset.left,
+                frame.y + inset.top,
+                @max(0, frame.width - inset.left - inset.right),
+                @max(0, frame.height - inset.top - inset.bottom),
+            );
+        }
+
+        /// Route pointer input into a bound terminal before ordinary press
+        /// dispatch. Ghostty's live terminal modes decide whether the child
+        /// receives encoded mouse reports or the emulator performs local text
+        /// selection (Shift overrides reporting for native selection/menu).
         fn handleTerminalPointer(self: *Self, runtime: *Runtime, pointer_event: core.CanvasWidgetPointerEvent) anyerror!bool {
             if (comptime !terminal_session.enabled) return false;
             switch (pointer_event.pointer.phase) {
-                .down, .move, .up, .cancel => {},
-                .hover, .wheel => return false,
+                .hover, .down, .move, .up, .cancel => {},
+                .wheel => return false,
             }
-            if (pointer_event.pointer.phase == .down and pointer_event.pointer.button != 0) return false;
             const target = pointer_event.target orelse return false;
             const layout = runtime.canvasWidgetLayout(pointer_event.window_id, pointer_event.view_label) catch return false;
             var terminal_node: ?canvas.WidgetLayoutNode = null;
@@ -4672,16 +4681,7 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
             const node = terminal_node orelse return false;
             if (!self.terminal_sessions.hasSession(node.widget.terminal.pty)) return false;
 
-            const frame = node.frame.normalized();
-            const padding = node.widget.layout.padding;
-            const declared = padding.left + padding.top + padding.right + padding.bottom > 0;
-            const inset: geometry.InsetsF = if (declared) padding else geometry.InsetsF.all(8);
-            const content = geometry.RectF.init(
-                frame.x + inset.left,
-                frame.y + inset.top,
-                @max(0, frame.width - inset.left - inset.right),
-                @max(0, frame.height - inset.top - inset.bottom),
-            );
+            const content = terminalContentRect(node);
             if (content.isEmpty()) return false;
             const result = self.terminal_sessions.pointerSelection(node.widget.terminal.pty, .{
                 .phase = pointer_event.pointer.phase,
@@ -4689,10 +4689,13 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
                 .y = pointer_event.pointer.point.y - content.y,
                 .width = content.width,
                 .height = content.height,
+                .button = pointer_event.pointer.button,
+                .modifiers = pointer_event.pointer.modifiers,
                 .click_count = pointer_event.pointer.click_count,
             });
             if (result.changed) try self.repaintTerminals(runtime, pointer_event.window_id);
-            return pointer_event.pointer.phase == .up and result.selection_active;
+            return result.mouse_reported or
+                (pointer_event.pointer.phase == .up and result.selection_active);
         }
 
         /// Typed press dispatch resolves through the press target — the
@@ -4705,7 +4708,7 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
         /// Msg), and any release/cancel disarms it.
         fn handlePointer(self: *Self, runtime: *Runtime, pointer_event: core.CanvasWidgetPointerEvent) anyerror!void {
             const tree = self.treeForViewLabel(pointer_event.view_label) orelse return;
-            const terminal_selected = try self.handleTerminalPointer(runtime, pointer_event);
+            const terminal_consumed = try self.handleTerminalPointer(runtime, pointer_event);
             switch (pointer_event.pointer.phase) {
                 .down => {
                     self.disarmHold(runtime);
@@ -4746,7 +4749,7 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
             }
             // A selection gesture owns the release the way a static
             // text drag does: keep its highlight and dispatch no press.
-            if (terminal_selected) return;
+            if (terminal_consumed) return;
             const target = pointer_event.press_target orelse return;
             // A released press on a synthesized fallback menu item is a
             // context-menu selection, not an ordinary press: it resolves
@@ -5739,6 +5742,22 @@ pub fn UiAppWithFeatures(comptime ModelT: type, comptime MsgT: type, comptime fe
             }
             const node = target orelse return false;
             if (!self.terminal_sessions.hasSession(node.widget.terminal.pty)) return false;
+            const content = terminalContentRect(node);
+            if (content.isEmpty()) return false;
+            if (self.terminal_sessions.mouseWheel(node.widget.terminal.pty, .{
+                .x = input_event.x - content.x,
+                .y = input_event.y - content.y,
+                .width = content.width,
+                .height = content.height,
+                .delta_x = input_event.delta_x,
+                .delta_y = input_event.delta_y,
+                .modifiers = .{
+                    .shift = input_event.modifiers.shift,
+                    .control = input_event.modifiers.control,
+                    .alt = input_event.modifiers.option,
+                    .super = input_event.modifiers.command or input_event.modifiers.primary,
+                },
+            })) return true;
             if (self.terminal_sessions.wheel(node.widget.terminal.pty, input_event.delta_y)) {
                 try self.repaintTerminals(runtime, input_event.window_id);
                 if (self.terminal_sessions.currentState(node.widget.terminal.pty)) |state| {
