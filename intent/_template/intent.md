# Intent: <short title>

Author: <name> (<team / role>) · Status: draft · Date: YYYY-MM-DD
External record: <tracker id or —>

## Problem

What cannot be done today, who is affected, and the cost of not
fixing it. 1–3 sentences in the originator's own words.

## Proposed outcome

What better looks like when this is done. Observable, not
implementation.

## Affected users and systems

Users, teams, surfaces, APIs, workspaces. E.g. claims handlers,
portal team, Herdr Workspace `target-agent`, `apps/web-ui` shell.

## Constraints

Non-negotiables the solution must respect. E.g. no new PII in portal
session, existing auth only, Herdr owns agent lifecycle, Rust owns
the durable ledger.

## Out of scope

Explicitly not doing, to prevent scope creep.

## Success criteria

How the product owner will know this succeeded (measurable where
possible).

## Open questions

- …

## Constraints from policy

Skills that will bound the spec: e.g. `herdr-authority`,
`rust-ledger`, `agent-factory-architecture`.

---

Review with the product owner before committing. An accepted intent
triggers `spec.md`.
